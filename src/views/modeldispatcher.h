#ifndef MODELDISPATCHER_H
#define MODELDISPATCHER_H

#include <models/model.h>

#include <memory>
#include <vector>

class ModelDispatcher {
public:
    ModelDispatcher();

    //    virtual std::vector<std::shared_ptr<Model>> getItems() = 0;
};

#endif // MODELDISPATCHER_H
